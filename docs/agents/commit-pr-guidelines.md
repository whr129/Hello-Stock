# Commit and PR Guidelines

Recent history uses short, imperative commits with prefixes like `fix:`. Follow that pattern, for example `feat: add search fallback` or `test: cover source config errors`.

PRs should include:

- a short description of the user-visible change
- any migration or `.env` changes
- test evidence, such as `48 passed`
- screenshots or sample bot output when Telegram responses changed

